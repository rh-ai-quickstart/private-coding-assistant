apiVersion: v2
name: pca-ai-serving
version: 0.1.0
description: PCA AI Serving — models, gateway, PVCs, inference, observability
dependencies:
  - name: pca-observability
    version: 0.1.0
    condition: observability.enabled
