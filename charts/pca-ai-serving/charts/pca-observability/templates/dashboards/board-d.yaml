{{- if and .Values.grafana.enabled .Values.langfuse.enabled }}
{{- $ns := include "pca-observability.namespace" . -}}
{{- $gpu := .Values.cost.gpuHourlyUsd -}}
{{- $ph := .Values.cost.gpuHourlyUsdIsPlaceholder -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: pca-grafana-dashboard-d
  namespace: {{ $ns }}
  labels:
    app.kubernetes.io/name: pca-grafana
    app.kubernetes.io/part-of: pca-observability
data:
  board-d.json: |
    {
      "uid": "pca-board-d",
      "title": "PCA D — Tokens / cost fairness",
      "tags": ["pca", "cost", "tokens", "fairness"],
      "timezone": "browser",
      "schemaVersion": 39,
      "version": 2,
      "refresh": "30s",
      "time": { "from": "now-24h", "to": "now" },
      "panels": [
        {
          "type": "text",
          "title": "Fairness & token cost",
          "gridPos": { "h": 4, "w": 24, "x": 0, "y": 0 },
          "options": {
            "mode": "markdown",
            "content": "Per-user / per-team token and $ cost live in **Langfuse** (model prices configured via Helm). Grafana shows aggregate Prometheus token rates plus a **PLACEHOLDER** GPU $/hr context ({{ $gpu }}{{ if $ph }} — override `cost.gpuHourlyUsd`{{ end }})."
          }
        },
        {
          "type": "timeseries",
          "title": "vLLM input tokens/sec (prompt / prefill)",
          "description": "Prompt tokens processed per second (prefill). Not used in performance-ladder output tokens/sec rates.",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 4 },
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "targets": [
            {
              "expr": "sum(rate(vllm:prompt_tokens_total[5m])) or sum(rate(vllm_prompt_tokens_total[5m]))",
              "legendFormat": "input tokens/sec (prompt / prefill)"
            }
          ]
        },
        {
          "type": "timeseries",
          "title": "vLLM output tokens/sec (decode / generation only)",
          "description": "Generation (decode) tokens per second — same token kind as the performance ladder output tokens/sec (ladder uses total stage time; this is a rolling 5m rate).",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 4 },
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "targets": [
            {
              "expr": "sum(rate(vllm:generation_tokens_total[5m])) or sum(rate(vllm_generation_tokens_total[5m]))",
              "legendFormat": "output tokens/sec (decode / generation only)"
            }
          ]
        },
        {
          "type": "stat",
          "title": "PLACEHOLDER — illustrative GPU cost ($/hour), not billing truth",
          "description": "Illustrative L40S on-demand from sizing doc. Set cost.gpuHourlyUsdIsPlaceholder: false when overridden with real rates.",
          "gridPos": { "h": 4, "w": 8, "x": 0, "y": 12 },
          "targets": [
            {
              "expr": "{{ $gpu }}",
              "instant": true
            }
          ],
          "fieldConfig": {
            "defaults": {
              "unit": "currencyUSD",
              "decimals": 2,
              "displayName": "{{ if $ph }}PLACEHOLDER {{ end }}$/GPU-hour (illustrative)"
            }
          }
        },
        {
          "type": "stat",
          "title": "Estimated GPU cost per day (1 GPU × placeholder $/hour)",
          "gridPos": { "h": 4, "w": 8, "x": 8, "y": 12 },
          "targets": [
            {
              "expr": "{{ $gpu }} * 24",
              "instant": true
            }
          ],
          "fieldConfig": { "defaults": { "unit": "currencyUSD", "decimals": 2 } }
        },
        {
          "type": "timeseries",
          "title": "vLLM finished inference requests per second",
          "description": "Completed vLLM inference requests per second. Not OpenCode total LLM model calls from the performance ladder.",
          "gridPos": { "h": 8, "w": 8, "x": 16, "y": 12 },
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "targets": [
            {
              "expr": "sum(rate(vllm:request_success_total[5m])) or sum(rate(vllm_request_success_total[5m]))",
              "legendFormat": "finished inference requests/sec"
            }
          ]
        }
      ]
    }
{{- end }}
