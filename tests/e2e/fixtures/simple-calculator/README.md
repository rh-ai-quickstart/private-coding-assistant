# simple-calculator

Tiny Python calculator used by PCA e2e tests.

- `calculator.py` — add / subtract / multiply / divide only (`power` is intentionally missing)
- `test_calculator.py` — includes power tests (`power(2, 4) == 16`, `power(3, 2) == 9`) that fail until the agent implements `power`
